# next.js
/node_modules
/.next/
/out/
next-env.d.ts
*.tsbuildinfo
.vercel
.superpowers/

# env files (never commit real credentials — .env.example is the only one tracked)
.env
.env.local
.env*.local

# local tooling / debug artifacts
.playwright-mcp/
.firecrawl/
*.png
